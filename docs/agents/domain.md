# Domain Docs

This repo uses a single-context domain doc layout.

## Read

- `CONTEXT.md` at the repo root, if present
- `docs/adr/`, if present

If these files do not exist, proceed silently. `/grill-with-docs` creates them lazily when terms or decisions are resolved.
